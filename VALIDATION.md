# 验收记录

## v1.3 更新验收

- 取消可信来源完整性门槛和 E/F 强制复查：有效池存在时零辅助查询，离线或新增来源不阻止四档判定；只有空池才补采，首个可用参考即停止。并发共享、空池限流、部分来源过期、全部过期、读写失败、信任撤销、暂停及刷新代际均有回归。
- 全部 Go 测试、`go vet ./...` 及既有前端行为检查通过；评级配置默认最近 60 分钟、3 次正式采样、5 分钟有效覆盖，旧配置补默认值，旧 API 请求保留已存新参数，显式最小覆盖 0 生效。
- 复现“历史 A + 连续 17 次待判定”：历史聚合 A 保留，当前解析质量改待判定、当前综合评级待评估，并保留近期性能评分和具体等待原因。
- 当前最新正式轮次按 F > E > unknown > clean > matched 汇总 A 记录，辅助可信查询不影响当前状态；旧 E/F 后最新恢复可即时恢复评级。混合 A/非 A、可信豁免、人工判定与缓存失效、未来记录、窗口边界、退避覆盖和 P95 均有回归。
- HTTP API 验证 24h/7d/30d 的历史指标与 current 独立共存，当前评级不随历史区间改变；参数范围、整数类型、最小覆盖不超过观察窗口均经过校验。
- Chrome 真实页面验证当前质量/评级筛选、默认 A→F 排序且待评估置后、详情和总览一致、CSV 当前评级窗口/采样/原因。将门槛改为 1 次/0 分钟立即使用已有证据评级，解析待判定不会因此升为 A。三个新输入框沿用固定单位、范围占位和行内纠错；390px 窄屏无横向溢出或单位重叠，无 JavaScript 错误。
- 隔离性能样本：100 台服务器、28,800 轮、57,600 条查询。包含新增 current 的 24h 汇总冷查询约 98.7ms，缓存查询低于该次计时显示精度，30d 图表查询约 1.0ms。数据为本机合成样本，不代表所有机器和数据规模；新增评级使用现存数据，不新增网络探测。
- 打开详情后，即使没有新探测，评级变化也随自动状态刷新更新；浏览器验证 A→待评估→A 仅使用一次历史图表请求，当前字段缺失时不回退到旧历史 A。
- TLS 校验维持原状。所有验收使用独立测试数据目录，未修改用户运行数据。

## v1.2 更新验收

以下为当时版本记录；其中全来源完整性门槛及 E/F 额外复核已在 v1.3 取消，以现有有效参考集合直接判定。

- 全部 Go 测试与 `go vet ./...` 通过；默认域名为 `www.youtube.com / A`，旧配置继续保留已有域名。可信历史默认 1 小时，0–720 小时可配置，0 关闭；旧配置缺失字段补默认值，旧 API 请求不清空新字段。
- 四档判定验证：新鲜参考一致、历史正常、同 /16 新地址为 E、跨 /16 新地址为 F、多 IP 取最差；无参考、失败和无 IPv4 结果保持待判定。可信角色免 E/F。
- TTL 验证包含真实 DOGGO JSON、CNAME 链最短 TTL、无关 owner 排除、缺失/零/无效 TTL、上限与过期边界。最终判定时刻单独记录，展示及导出可追溯。
- 每域名共享常规与异常参考采集，常规最短 30 秒、异常额外复核冷却 300 秒。并发 24 个目标共享采集的回归通过；所有子进程仍受全局并发上限约束。
- 参考池重启恢复、历史关闭/到期、来源与域名隔离、信任撤销/禁用/改址、CSV 覆盖清除及 256/16384 条容量限制通过；旧在途查询不能在撤销后重新填池。
- 暂停、手动刷新代际变化、保存失败、采集中新增可信源、采集后 TTL 恰好过期等边界回归通过：参考不完整时不自动评 E/F。
- 用 v1 实际旧表结构验证迁移：旧自动 F 保留原始证据，有效状态改待判定；人工污染及人工取消保持有效。切回自动不会恢复旧规则 F。30 天清理覆盖原始结果、按各观察时刻过期的嵌套参考及可信池。
- 8 台本机 UDP DNS 使用真实 DOGGO 联调，验证四档、混合答案、空回答；关闭并重启最终二进制后，预先保存的 TTL 已过期可信历史仍可正确命中。
- Chrome 验证四档筛选、A–F 排序、E 汇总 CSV、原始 TTL 证据 CSV、旧规则说明、人工正常/污染/恢复自动、可信豁免、历史配置保存、模板下载及桌面/390px 布局。无页面 JavaScript 错误。
- 最终便携包在中文与空格路径中启动成功，默认域名及历史配置正确；Windows PowerShell 5.1 构建及原生解压验证通过，两个中文文档名称正确且内容与源码一致。

## v1.1 更新验收

- 手动刷新实际启动一轮启用服务器的探测，网页显示进度；重复点击合并同一批次，超过 256 台的队列、在途服务器补探及中途暂停均有验证。
- 暂停或停止导致可信参考采集中断时，保存已完成证据并标记未知，避免不完整并集造成误判；暂停与取消回归测试通过。
- 新配置默认探测 `youtube.com`。已有用户配置保持不变。
- 可信 DNS 的历史与当前展示均不判污染、不评 F；可信回答取并集，目标回答为并集子集时通过，含集合外地址时标记污染。多个明确设为可信的地址别名仍参与参考。
- 评级双向排序已在浏览器验证，待评估始终排在最后。
- 三种 CSV 用途分开：总览导出当前统计区间内、按当前筛选和排序的监测汇总；服务器清单导出用于迁移；原始记录从使用指南或服务器详情导出。已验证 UTF-8 BOM、公式转义及筛选后仅导出可见记录。
- CSV 导入已验证模板、IPv6 拒绝、地址别名去重、文件内重复、逐项覆盖/跳过、全部覆盖/跳过、历史数据保留、缺省可选列保留、过期预览冲突及事务回滚。
- 排版参考 Apple HIG 的可读性与层级原则，使用 Windows 系统字体。浏览器实测正文 16px、表格主要文字 15px、辅助文字至少 13px；增大控件与行距，改善次要文字对比度。已检查桌面及 390px 窄屏截图，页面本身无横向溢出；宽表格保留内部横向滚动。
- 真实 DOGGO 与 5 台本机 UDP DNS 联调，验证完整可信并集、子集、集合外地址及手动探测结果更新。Chrome 浏览器验收没有页面 JavaScript 错误。

下方开销测量与公共网络结果为初版实测记录，不视为 v1.1 的重新测量。


日期：2026-09-18。运行平台：Windows amd64。构建工具：Go 1.27.1；DOGGO：随项目提供的 v1.4.0。最终发行物为 `dist/DNSMonitor-windows-amd64.zip`。

## 已完成

- `go test ./...` 与 `go vet ./...` 通过。
- 地址校验、IPv4 与环境隔离、DNS stamp 支持范围、默认端口别名去重、DOGGO JSON 解码、可信参考并集、全局并发、暂停与取消、离线退避测试通过。
- 使用真实 DOGGO 查询本机 UDP DNS；通过完整应用验证正常、超时、可信比对污染、人工清除、人工定罪、恢复自动判定、历史聚合、CSV 导出、并发 0 暂停。
- SQLite 测试覆盖持久化、退避期间时间加权、覆盖空档、人工判定及审计、原始与嵌套参考证据超期删除、删除级联、同毫秒分页、流式导出期间继续访问数据库。
- Chrome 无头浏览器验证登录、筛选、24h/7d/30d、Canvas 图表、污染证据与操作菜单、服务器增删改、配置保存、服务状态页面及手机宽度布局；无页面 JavaScript 错误。
- 将最终 ZIP 解压至包含中文和空格的不同目录后成功启动，确认数据落在程序旁、默认配置正确、发行包无测试数据、同数据目录的第二实例被拒绝、启动失败记录进入日志。
- 少量公开网络验证：阿里 DNS 的 UDP、DoH、DoT 均收到成功的 NOERROR 回答，单次 RTT 分别为 7、57、254ms。只是当时本机网络的连通性结果，不是长期质量评价。

## 开销测量

真实进程短时采样场景：4 台本机测试 DNS，2 个域名，5 秒周期，并发 2，其中包含超时节点。采样 11.77 秒，主程序 CPU 累计 0.125 秒（约占一个逻辑核心 1.06%），主程序工作集峰值约 23.81 MiB；观察到的主程序与 DOGGO 合计工作集峰值约 35.16 MiB。

以上是短时采样；瞬时子进程可能未被采到，不代表所有协议和规模下的内存上限。默认周期为 300 秒。操作系统、缓存、并发、TLS、域名数量和历史记录会影响实际开销。

合成存储基准：100 台服务器 × 288 轮 × 2 个域名，共 57,600 条原始记录；写入 9.894 秒，数据库约 23.47 MiB；24 小时统计冷查询约 91.78ms；30 日图表查询约 1.06ms，返回 720 个点（数据库中只有 24 小时的合成数据）。缓存统计耗时低于该计时器分辨率。该基准不代表真实 DNS 网络性能，也没有拿短记录大小承诺一个月磁盘空间。

## 边界

- Windows 服务状态查询、控制代码、管理员辅助进程与单实例机制已经实现；验收未在本机安装或启动 Windows 服务。实际服务安装/卸载及完整 SCM 启停流程未做破坏性现场测试。
- DoH3、DoQ、DNSCrypt 的地址与参数适配有单元验证，本次没有可用测试端点逐一完成真实网络连通性验收。
- 没有宣称 DOGGO 完成完整 DNSSEC 签名链验证。可信身份由用户指定。
- 带固定 IP、证书哈希或 bootstrap 约束的加密 DNS stamp 会明确拒绝；不静默忽略原始约束。AdGuard Home 分域路由规则不作为单服务器地址接受。
- DOGGO 无法在 JSON 中表达的空响应码标记 UNKNOWN，不用于污染定罪；输出超过限制时保存错误与截断证据，不用于比对。

浏览器验收截图和临时测试数据位于工作目录 `.test-output`，不会进入便携 ZIP 或 Git。
